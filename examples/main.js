// Dev/demo entry — mounts the examples gallery. This is consumer code, not part
// of the Scramble component.
import { createApp } from 'vue';
import Gallery from './Gallery.vue';

createApp(Gallery).mount('#app');
